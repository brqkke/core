query me {
    me {
        ...UserProfile
    }
}

fragment BityStatus on User{
    id
    bityTokenStatus {
        linked
        linkStatus
    }
}


fragment UserProfile on User{
    ...BityStatus
    id
    locale
    email
    vaults {
     ...VaultInfos
    }
}
