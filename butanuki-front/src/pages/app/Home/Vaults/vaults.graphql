query vaults{
    me{
        id
        vaults {
            ...VaultInfos
        }
    }
}

mutation deleteOrder($orderTemplateId: ID!){
    deleteOrderTemplate(orderTemplateId: $orderTemplateId){
        id
    }
}
